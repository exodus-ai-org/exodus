// ARCHIVED: Settings moved from dialog to /settings route.
//
// The original dialog composed <SettingsSidebar> + <SettingsForm> inside a
// shadcn <Dialog>. Now these same components are used in
// src/renderer/containers/settings.tsx as a full page.
